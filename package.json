{
  "name": "live-commerce-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Live Commerce Platform - Monorepo",
  "workspaces": [
    "packages/*",
    "client-app",
    "admin-app",
    "backend"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=client-app",
    "dev:admin": "npm run dev --workspace=admin-app",
    "dev:backend": "npm run start:dev --workspace=backend",
    "dev:all": "concurrently \"npm run dev:client\" \"npm run dev:admin\" \"npm run dev:backend\"",
    "build:client": "npm run build --workspace=client-app",
    "build:admin": "npm run build --workspace=admin-app",
    "build:backend": "npm run build --workspace=backend",
    "build:shared": "npm run build --workspace=@live-commerce/shared-types",
    "build:all": "npm run build:shared && npm run build:client && npm run build:admin && npm run build:backend",
    "type-check:client": "npm run type-check --workspace=client-app",
    "type-check:admin": "npm run type-check --workspace=admin-app",
    "type-check:backend": "npm run build --workspace=backend",
    "type-check:shared": "npm run type-check --workspace=@live-commerce/shared-types",
    "type-check:all": "npm run type-check:shared && npm run type-check:client && npm run type-check:admin && npm run type-check:backend",
    "test:backend": "npm run test --workspace=backend",
    "test:e2e": "npm run test:e2e --workspace=backend",
    "test:all": "npm run test:backend && npm run test:e2e",
    "lint:client": "npm run lint --workspace=client-app",
    "lint:admin": "npm run lint --workspace=admin-app",
    "lint:backend": "npm run lint --workspace=backend",
    "lint:all": "npm run lint:client && npm run lint:admin && npm run lint:backend",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "prisma:generate": "npm run prisma:generate --workspace=backend",
    "prisma:migrate": "npm run prisma:migrate:dev --workspace=backend",
    "prisma:studio": "npm run prisma:studio --workspace=backend",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules",
    "postinstall": "npm run build:shared"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
